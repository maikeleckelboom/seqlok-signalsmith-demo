{
  "name": "@seqlok/commands",
  "version": "0.3.0-next.0",
  "description": "Typed command transport on top of Seqlok primitives (frozen snapshot).",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist/**/*", "UPSTREAM.md"],
  "dependencies": {
    "@seqlok/base": "workspace:*",
    "@seqlok/primitives": "workspace:*"
  }
}
